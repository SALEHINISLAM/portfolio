import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
    return (
        <div>
            <h1>footer</h1>
        </div>
    );
};

Footer.propTypes = {
    
};

export default Footer;